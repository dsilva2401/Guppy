module.exports = function ($methods, $engines, $database, $q, $config, $global, $app) {
	
	// Dependencies
		var $ = {};
		$.database = $database;
		$.q = $q;
		$.config = $config;
		$.global = $global;
		$.methods = $methods;
		$.app = $app;

	// Engines
		// $engines.SystemData = require('./SystemData')($);

}